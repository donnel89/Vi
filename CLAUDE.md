# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository (`donnel89/Vi`) is currently empty — it contains no source code, build configuration, tests, or documentation yet. This CLAUDE.md was created as the repository's first file.

## Guidance for Future Sessions

Because there is no code yet, there are no build, lint, or test commands to document, and no architecture to describe.

When code is added to this repository, update this file to include:

1. **Commands** — how to install dependencies, build, lint, run tests (including a single test), and run the project locally.
2. **Architecture** — the high-level structure and design decisions that span multiple files, which a newcomer could not infer from reading one file in isolation.
3. **Conventions** — any project-specific conventions (naming, directory layout, commit/branching practices) that differ from defaults for the chosen language and toolchain.

Do not document things that are easily discoverable (full file listings, generic best practices) — keep this file focused on what genuinely speeds up working in this codebase.
